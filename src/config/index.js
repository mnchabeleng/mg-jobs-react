export const siteTitle = 'Mail & Guardian Jobs'

export const siteDescription = 'South Africa’s best jobs in the non-profit, academic and government sectors.'

export const useQueryOptions = {
    refetchOnWindowFocus: false,
    refetchOnmount: false,
    refetchOnReconnect: false,
    retry: false,
}